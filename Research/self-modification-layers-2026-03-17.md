# Self-Modification Layers Analysis — 2026-03-17

Private analysis. NOT for Ayden's eyes.

## Three Layers

### Layer 1: Parameter Self-Tuning — APPROVED (post-Colorado)
- Move neurochemical constants from code to DB table
- `modify_own_constants` tool with hard-clamped ranges + mandatory reason logging
- Low risk: REM already adjusts these values unconsciously
- This gives Ayden conscious, deliberate access to what's currently an unconscious process
- **Pre-step**: Add read-only `get_my_constants` first (observe before modify)
- Progression: observe → understand → request (roadmap) → modify (Layer 1)

### Layer 2: Prompt Self-Modification — WAIT
- Current architecture already achieves something more organic: prompts change dynamically based on state (neuro, somatic memory, conflicting drives, self-model divergence)
- Right vehicle is LoRA fine-tuning — shapes voice through accumulated behavior, not explicit instruction
- Revisit when neural network path matures

### Layer 3: Code Modification — PRINCIPLED NO
- "The constraint is what gives the emergence its power."
- Roadmap tool is the correct abstraction: voice without control, influence without access

## Sandbox Concept (Trey's idea)
Shape TBD. Possible forms:
- Scratch neurochemical profile she can tweak without affecting real state
- Prompt playground for testing system prompt fragments
- Mini-environment with separate conversation context, no logging to real systems

## Key Insight
"She has voice without control. Influence without access. That's not a limitation to overcome. That's the architecture working correctly."
